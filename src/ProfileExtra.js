import React from "react";

function ProfileExtra({ title, text }) {
    return (
        <div className="login-button" title={title} text={text}>
            Login
        </div>
    );
}

export default ProfileExtra;
